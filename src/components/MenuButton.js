import React from 'react';
import FaIcon from 'react-native-vector-icons/FontAwesome5';

const MenuButton = (props) => 
            <FaIcon.Button {...props} name="bars" color={'#FFFFFF'}/>

export default MenuButton;